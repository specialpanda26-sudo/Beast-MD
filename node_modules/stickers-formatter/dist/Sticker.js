"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSticker = exports.Sticker = void 0;
const fs_1 = require("fs");
const promises_1 = require("fs/promises");
const Utils_1 = __importStar(require("./Utils"));
const convert_1 = __importDefault(require("./internal/convert"));
const Exif_1 = __importDefault(require("./internal/Metadata/Exif"));
const StickerTypes_1 = require("./internal/Metadata/StickerTypes");
const _1 = require(".");
const file_type_1 = require("file-type");
class Sticker {
    data;
    metadata;
    constructor(data, metadata = {}) {
        this.data = data;
        this.metadata = metadata;
        this.metadata.author = this.metadata.author ?? '';
        this.metadata.pack = this.metadata.pack ?? '';
        this.metadata.id = this.metadata.id ?? Utils_1.default.generateStickerID();
        this.metadata.quality = this.metadata.quality ?? 100;
        this.metadata.type = Object.values(StickerTypes_1.StickerTypes).includes(this.metadata.type)
            ? this.metadata.type
            : StickerTypes_1.StickerTypes.DEFAULT;
        this.metadata.background = this.metadata.background ?? Utils_1.defaultBg;
    }
    _parse = async () => {
        if (Buffer.isBuffer(this.data))
            return this.data;
        if (this.data.trim().startsWith('<svg'))
            return Buffer.from(this.data);
        if ((0, fs_1.existsSync)(this.data))
            return (0, promises_1.readFile)(this.data);
        const res = await fetch(this.data);
        if (!res.ok)
            throw new Error(`Failed to fetch: ${res.statusText}`);
        return Buffer.from(await res.arrayBuffer());
    };
    _getMimeType = async (data) => {
        const type = await (0, file_type_1.fileTypeFromBuffer)(data);
        if (!type) {
            if (typeof this.data === 'string')
                return 'image/svg+xml';
            throw new Error('Invalid file type');
        }
        return type.mime;
    };
    build = async () => {
        const data = await this._parse();
        const mime = await this._getMimeType(data);
        return new Exif_1.default(this.metadata).add(await (0, convert_1.default)(data, mime, this.metadata));
    };
    toBuffer = this.build;
    get defaultFilename() {
        return `./${this.metadata.pack}-${this.metadata.author}.webp`;
    }
    toFile = async (filename = this.defaultFilename) => {
        await (0, promises_1.writeFile)(filename, await this.build());
        return filename;
    };
    setPack = (pack) => {
        this.metadata.pack = pack;
        return this;
    };
    setAuthor = (author) => {
        this.metadata.author = author;
        return this;
    };
    setID = (id) => {
        this.metadata.id = id;
        return this;
    };
    setCategories = (categories) => {
        this.metadata.categories = categories;
        return this;
    };
    setType = (type) => {
        this.metadata.type = type;
        return this;
    };
    setQuality = (quality) => {
        this.metadata.quality = quality;
        return this;
    };
    setBackground = (background) => {
        this.metadata.background = background;
        return this;
    };
    /** @deprecated Use `.build()` instead */
    get = this.build;
    toMessage = async () => ({ sticker: await this.build() });
    static extractMetadata = _1.extractMetadata;
}
exports.Sticker = Sticker;
const createSticker = async (...args) => new Sticker(...args).build();
exports.createSticker = createSticker;
