"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const child_process_1 = require("child_process");
const promises_1 = require("fs/promises");
const os_1 = require("os");
const util_1 = require("util");
const execAsync = (0, util_1.promisify)(child_process_1.exec);
const videoToGif = async (data) => {
    const filename = `${(0, os_1.tmpdir)()}/${Math.random().toString(36)}`;
    const video = `${filename}.video`;
    const gif = `${filename}.gif`;
    await (0, promises_1.writeFile)(video, data);
    try {
        await execAsync(`ffmpeg -i "${video}" "${gif}"`);
        return await (0, promises_1.readFile)(gif);
    }
    finally {
        await Promise.allSettled([(0, promises_1.unlink)(video), (0, promises_1.unlink)(gif)]);
    }
};
exports.default = videoToGif;
