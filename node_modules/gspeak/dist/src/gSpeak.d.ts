import { PassThrough } from 'stream';
export type SaveCallback = (err: Error | null) => void;
export declare class gSpeak {
    private lang;
    private text;
    private text_parts;
    private debug;
    constructor(text: string, lang?: string, debug?: boolean);
    private getHeaders;
    private getUrl;
    stream(): PassThrough;
    save(filePath: string, callback: SaveCallback): void;
    private _tokenize;
    private _minimize;
    static get languages(): Record<string, string>;
}
export default gSpeak;
