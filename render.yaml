services:
  - type: web
    name: beast-bot-ogolla
    env: docker
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 5000
      - key: WEB_PORT
        value: 3000
    healthCheckPath: /status
